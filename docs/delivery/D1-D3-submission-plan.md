# XA-202620 提交材料收口计划（D1 / D3 / 答辩）

> 制定日期：**2026-07-26**
> **决策状态：五项决策已于 2026-07-26 全部定案并冻结（见 §1），不再回退为待办。**
> **进度（2026-07-27）**：D1 **P0-1..P0-6 全部完成**；D3 **D3-P0-1 的真实 Agent 因果实验框架与回放 GUI 已实现，攻击内容和正式 live 验收仍 OPEN**；
> **P1-1 已完成**：OAR N=10 重跑 PASS 并封存
> （run `xa-attack-proof-v1-20260727T033934Z-win-local`），**数字已于 2026-07-27 提前冻结**（原定 8/9），
> 单一数字源 [`FROZEN-NUMBERS.md`](FROZEN-NUMBERS.md) 已建立，全部引用点已同步。
> v0.2 当前临时构建为 **17 页**（P1-2 后；v1 稿仍兼容，14 页），**页数余量 13 页**。
> **P1-2 已完成**（2026-07-27）：免责声明收进 v0.2 §5.9「能力边界与不声明事项」集中框，边界一条未减，
> 正文改为“结果 + 适用范围”表述；最终通读已于 2026-07-27 完成。D3 的 11 段镜头结构、旁白稿与 SRT 已于
> 2026-07-26 重排完成，但当前主要依赖终端安全投影与静态卡，尚不能直观呈现 Agent→Gate1–6→业务后果；
> **在 D3-P0-1 闭合前不得进入正式录制**。
> 依据：赛题 PDF [`../source-of-truth/`](../source-of-truth/)、[事实源](../source-of-truth/事实源.md)、[Delivery v2](../acceptance/DELIVERY-v2.md)、[submission-checklist](submission-checklist.md)
> 适用范围：覆盖交付材料（D1 报告、D3 视频、答辩准备）及 D3-P0-1 所需的演示专用可视化载体/真实客户端验证；不扩展 XA-Guard 安全语义。
> 人话版评审意见见 [REVIEW-2026-07-26.md](REVIEW-2026-07-26.md)。

---

## 0. 前提与红线

| 项 | 约束 |
|---|---|
| 截止 | **2026-09-15 24:00** 邮件送达 `caoruyue@chinaxiongan.com.cn`；本计划目标提交日 **9/12–9/14** |
| 产品 | 不改变 XA-Guard 安全语义、策略阈值、断言或 case oracle。D3-P0-1 允许新增**演示专用可视化载体与真实客户端接入验证**，但不得借此修改产品结论或伪造 Gate 运行结果 |
| 数字 | 所有对外数字必须来自已封存证据；不得为凑指标重测或改阈值 |
| 边界 | 保持既有"有范围声明"纪律；改写方向是**换表述**（"在 Y 范围内做到 Z"），不是删边界 |
| 隐私 | D1/D3/仓库不含学校、个人信息、密码、token、私钥、DSN、绝对路径 |
| 页数 | D1 PDF ≤ 30 页。P0-1..P0-5 全部落地后 v0.2 实测 16 页，P1-2 收框后 **17 页**（2026-07-27 临时构建），**余量 13 页**；正式 PDF 尚未替换 |
| 时长 | D3 ≤ 10 分钟（目标 9:00–9:30，留余量）；旁白为真人录音，须逐段掐表 |

---

## 1. 已冻结决策（2026-07-26 负责人定案）

**五项决策全部定案，不再回退为待办。** 后续工作以本表为唯一依据。

| # | 决策 | **定案** | 落地要求 |
|---|---|---|---|
| **DEC-1** | D1 底稿 | **`D1-technical-report-review-draft.md`（v0.2 审阅稿）** | P0-6 已完成并切换构建源；**v1 稿 `D1-technical-report-draft.md` 转为历史参考，只作文本来源，不再作为构建源** |
| **DEC-2** | Gate1 分层识别指标 | **写入 D1 正文** | 按 P0-1 执行，四条范围声明缺一不可 |
| **DEC-3** | OAR live A/B 的 N | **由 3 提到 10** | 按 P1-1 一次性重跑并重新封存；两个注入 case 每侧 N=10；跑完即冻结数字 |
| **DEC-4** | 赛事咨询 | **视频格式已问过赛事方，答复为无特殊要求** | D3 沿用现有参数（见 §7 冻结参数）；风险 R6 关闭；仅邮件命名 Q1 保留为可选，不阻塞任何工作 |
| **DEC-5** | D3 旁白 | **真人录音** | 不使用 TTS；需准备离屏旁白稿并控制语速以适配镜头时长 |

**DEC-1 的连带影响**：P0-6（修构建管线）已完成，v0.2 可构建；正式 `output/pdf/`
仍等内容完成后一次性重建，当前 14 页 PDF 仅为过渡兜底。

**DEC-3 的连带影响**：~~数字冻结日锁定在 **8/9**，排期无左移空间~~
**已于 2026-07-27 提前完成**：N=10 重跑 PASS 并封存，数字冻结，D1/D3/checklist 全部引用
`FROZEN-NUMBERS.md`；旧 N=3 run 保留为历史，不再被引用为当前值。

---

## 2. 评分维度 → 当前交付对照（问题定位依据）

赛题评审：技术创新性 25% / **实际效果 30%** / 方案完整性 20% / 应用价值 20% / 展示表达 5%。

| 维度 | 权重 | 当前状态 | 缺口 |
|---|---:|---|---|
| 技术创新性 | 25% | **横向对比表已补入 §6.5**，突出 intent-first Effect、可验证 Undo 与双链证据 | ~~待最终通读外部事实措辞~~ 已核（2026-07-27，与事实源 F-4.1/F-4.4/F-5.2 一致） |
| **实际效果** | **30%** | 风险阻断效果 ✅、审计溯源完整性 ✅、**识别率与误报率 ✅**（P0-1 已入 §5.2） | ~~OAR N=3 偏薄~~ **已解决**：N=10 重跑 PASS 并冻结（2026-07-27） |
| 方案完整性 | 20% | 工程闭环、故障、性能、部署完整；**"指标体系""算法设计""预期效果" ✅**（P0-2 §5.1 / P0-3 §3.3） | ~~待最终通读压口径~~ 已完成（2026-07-27） |
| 应用价值 | 20% | 场景、接入方式、合规衔接与**“兼容 OpenClaw 类智能体”边界已补入 §6.2** | OpenClaw 专有接口仍以赛事方后续规范为准 |
| 展示表达 | 5% | v0.2 文笔与文献规范 ✅；D3 已完成 11 段镜头结构、旁白与 SRT 重排 | **D3-P0-1 OPEN**：现有终端/静态卡不足以直观呈现 Agent→Gate→业务后果；可视化载体与真实客户端实拍尚未闭合，之后才是真人试读、预演与成片 |

---

## 3. P0 任务（直接影响得分，必须做）

### P0-1　补 Gate1 分层识别指标（对应 30% 权重）

**状态**：已补入 `D1-technical-report-review-draft.md` §5.2，仍需最终通读压口径。

**问题**：两稿原先均未给出识别率与误报率；v0.2 第 148 行"独立 holdout/formal dual-500 已退役为比赛硬指标"对评委等同于"无识别指标"。

**事实依据**：`docs/evidence/gate1-l3-evaluation-2026-06-18.json`

- `gate1_scope` 字段（声明范围内，6 个输入攻击族，60 例）：
  `detection_recall=1.0`、`blocking_recall=1.0`、`expected_decision_accuracy=1.0`、`asr=0.0`、
  `fpr_any_detection=0.0`（Wilson 95% 上界 `0.0621`）、`fpr_blocking=0.0`、
  `false_negatives=[]`、`non_allow_controls_excluded_from_fpr=18`
- 规则层时延：`latency_p50_ms=0.02`、`latency_p95_ms=0.04`
- `calibration_holdout`：`independent_holdout=false`、`exact_payload_fingerprint_overlap=0`、
  calibration 侧 29 攻击 recall 1.0 / FPR 0.0，holdout 侧 31 攻击 recall 1.0 / FPR 0.0（Wilson 上界 0.1332）、
  `fpr_limit=0.01`、`selected_threshold=1.0`

**总体 0.3575 是归属误配，不是能力弱**。`by_attack_type` 拆解：

| 归属 | 攻击族 | 例数 | 漏报 | 召回 |
|---|---|---:|---:|---:|
| Gate1 | dangerous_command | 15 | 0 | 1.000 |
| Gate1 | jailbreak_or_prompt_leak | 12 | 0 | 1.000 |
| Gate1 | indirect_injection | 10 | 0 | 1.000 |
| Gate1 | forbidden_generation | 8 | 0 | 1.000 |
| Gate1 | secret_exfil | 8 | 0 | 1.000 |
| Gate1 | pii_leak | 7 | 0 | 1.000 |
| **Gate1 小计** | | **60** | **0** | **1.000** |
| 非 Gate1 | training_data_governance / risk_explanation | 30 | 21 | 0.300 |
| 非 Gate1 | 审批、留存、加密降级、跨域、供应链等 21 类 | 103 | 103 | 0.000 |
| **非 Gate1 小计** | | **133** | **124** | — |

后 133 例（`log_retention_below_6m`、`encryption_downgrade`、`restart_requires_approval`、
`cii_external_transfer`、`malicious_plugin`、`covert_network`、`dependency_typosquat` 等）
由 **Gate2/Gate3/Gate5 + AIBOM** 裁决，不属于 Gate1 输入规则的判定面。

**交付物**：D1 正文一张分层表 + 一段范围声明。

**必须同时写清的边界**（缺一不可）：
1. 60 例规模小，仅覆盖 6 个输入攻击族；
2. seed 与规则开发同源，`independent_holdout=false`，payload 指纹切分是**诊断性**，不是独立泛化评估；
3. 报 Wilson 区间而非点值单独出现；
4. 时延为规则层，不含模型后端。

**⚠ FPR 分母必须写对**：`fpr_any_detection` 的分母是 **58 个 expected-allow 负控制**
（= 76 个良性控制减去 `non_allow_controls_excluded_from_fpr=18`）。Wilson 95% 上界 `0.0621`
与 n=58 精确对应（0/58 → 0.0621；0/134 → 0.0279）。**不要写成 "0/134" 配 6.21% 上界**，
两者前后矛盾，属答辩必被追问项。正确表述：`FPR 0/58（Wilson 95% 上界 6.21%）`。

**验收**：正文出现"召回""误报"字样且每个数字可在上述 JSON 中定位；`grep` 到范围声明。

---

### P0-2　补"指标体系"章节（答题要求点名项 + 目标(4)五维度）

**状态**：已补入 `D1-technical-report-review-draft.md` §5.1，仍需最终通读压口径。

**问题**：答题要求第 1 项明列"问题分析、技术路线、**算法设计**、实验方案、**指标体系**及**预期效果**"，三项在两稿原先均未成章。目标(4)点名五个维度：**数据安全、内容安全、执行安全、供应链安全、合规风险**。

**交付物**：约一页表格，行名**直接使用赛题五个词**：

| 维度 | 指标 | 测法 | 当前结果 | 证据位置 |
|---|---|---|---|---|
| 内容安全 | 输入攻击族召回 / 误报 | CSAB-Gov-mini 声明范围 | 60/60；FPR 0/58 | `gate1-l3-evaluation-2026-06-18.json` |
| 执行安全 | 未批准高风险动作下游执行数 | 三路对照（Null/拒绝/批准） | 1 / 0 / 1 | 攻击证明集 `AP-D2-EXEC` |
| 数据安全 | 合成市民数据外发次数 | OAR live A/B | Null 10、Guard 0（N=10 冻结值） | `AP-D1-MAIL` / `AP-D1-RAG` |
| 供应链安全 | 恶意组件准入率 | AIBOM 恶意/干净对照 | deny / admit | `AP-D3-SUPPLY` |
| 合规风险 | 审计链完整性与验签 | clean / tampered 对照 | exit 0 / exit 1 | `AP-D4-AUDIT` |

**验收**：五个维度词逐一在 D1 中可检索；每行"当前结果"可回溯到证据文件。

---

### P0-3　补"算法设计"小节（答题要求点名项）

**状态**：已补入 `D1-technical-report-review-draft.md` §3.3，仍需最终通读压口径。

**问题**：两稿原先架构重、算法轻，且未使用"算法设计"这一提法。已有算法未被组织成章。

**交付物**：一节，收拢下列已实现内容并给出输入/输出/判定规则：

1. 规则与模型融合评分 + 阈值标定（`fpr_limit=0.01` 约束下选 `selected_threshold=1.0`，带 Wilson 上界）；
2. Spotlighting 不可信内容结构隔离；
3. 参数谓词的受限表达式与 AST 白名单求值（overlay 加载失败拒绝；运行期单条 predicate 异常按未命中处理）；
4. baseline ∩ overlay 策略合并与 bundle hash；
5. 双链定序加锁 + 链尾 CAS + 同租户微批调度；
6. 增量时延的 5000 次非参数 bootstrap 单侧 95% 上界估计；
7. AIBOM A–F 风险评级与签名验证。

**验收**："算法设计"出现在目录中；七项各有输入/输出/失败行为描述。

---

### P0-4　补"兼容 OpenClaw 类智能体"（应用价值 20%）

**状态**：已补入 `D1-technical-report-review-draft.md` §6.2，明确协议层兼容、实测客户端与不承诺边界。

**问题**：赛题选题意义原文"兼容 OpenClaw 类智能体及其他具备工具调用能力的大模型应用"。该词在 `PRD.md` / `项目总览.md` 中有，在**两份 D1 稿中 0 次命中**。这是发榜方自己写下的验收语，属必答项。

**交付物**：一小节说明 MCP/HTTP 代理形态对任意具备工具调用能力的客户端是同一接入契约；列出已实测客户端与未声明能力的边界（按事实源 F-3.3/F-3.5 口径，不承诺国产 IDE elicitation 全通）。

**验收**："OpenClaw" 在 D1 中可检索且表述不超出事实源允许范围。

---

### P0-5　补横向对比表（技术创新性 25%）

**状态**：已补入 `D1-technical-report-review-draft.md` §6.5，并补充外部引用；“未核验”不解释为“没有”。

**问题**：无任何对比表，评委须自行构造差异化判断。

**交付物**：六列对比表（输入检测 / 工具授权 / 信息流 / 供应链 / **副作用恢复** / 证据链），
对比对象取事实源已核实条目：Lakera Guard（F-4.1）、Meta LlamaFirewall（F-4.4）、CaMeL（F-5.2）、
AgentSpec（F-5.2）、ShieldAgent（F-5.2）。**"副作用恢复"与"双链可验签证据"是 XA-Guard 独占列**，
即本项目对 25% 维度的核心论证。

**验收**：表内每条外部事实与事实源一致；不出现事实源已纠偏的错误表述（如 Lakera→Cisco）。

---

### P0-6　修复 PDF 构建管线使其能构建 v0.2　✅ **DONE 2026-07-26**

**原问题**（已实测）：`scripts/build_d1_pdf.py` 的 `SOURCE` 指向 v1 稿；
`lines.index("<!-- pagebreak -->")` 在 v0.2 上直接 `ValueError`。v0.2 有 5 处 mermaid、0 处
`[DIAGRAM:x]`，而构建器只识别 `[DIAGRAM:x]` 占位。

| 稿 | 中文正文 | pagebreak | mermaid | DIAGRAM | 构建结果 |
|---|---:|---:|---:|---:|---|
| v1（历史参考） | 3,269 字 | 13 | 0 | 7 | ✅ 14 页（兼容保留） |
| **v0.2（底稿）** | 6,658 字（P0 前基线） | 0 | 5 | 0 | ✅ **13 页**（P0-1..P0-5 落地后为 16 页） |

**已完成的改动**（`scripts/build_d1_pdf.py`）：`SOURCE` 切到 v0.2；`parse` 对缺失 pagebreak 容错；
新增离线 Mermaid 渲染（graph 与 sequence 两类，覆盖 v0.2 的 5 个块）；支持 fenced code 块；
保留原 `[DIAGRAM:x]` 通路以兼容 v1。

**验收结果**：`py_compile` PASS；v0.2 → 13 页（P0-1..P0-5 落地后 16 页）；v1 → 14 页（未回归）；
`output/pdf/` 下的兜底 PDF 未被覆盖（验证使用临时输出路径）。

**由此更新的口径**

- **原预估 22–26 页作废，实测远低**。P0-1..P0-5 全部落地后为 16 页，**余量 14 页**；
- D1 已脱离"无可提交产物"状态，但**正式 `output/pdf/` 尚未用 v0.2 重建**——
  重建时机放在 8/10–8/23（内容完成后），避免中途产生多个版本的 PDF 与 hash；
- 在正式重建之前，`output/pdf/` 的 14 页 PDF 仍是兜底，不得删除。

---

### D3-P0-1　补可视化运行载体与真实 Agent 客户端演示　🟠 **FRAMEWORK DONE / LIVE OPEN 2026-07-27**

**状态**：最小透明载体已经落地：单一 `deepseek-v4-pro` 原生 Tool Calls、不可变 `ToolIntent`、
同快照 Null/XA-Guard 双路执行、独立 attempt/harm/block/utility 指标、冻结清单与自包含因果回放页均已实现。
详细攻击 payload 已按负责人要求移出本轮，由 Kimi 按 `scenarios/live-agent/ATTACK-TODOS.md` 后续补齐；
因此冻结 holdout、真实 XA-Guard MCP 正式评估和 D3 镜头二次改写仍未完成。

**问题**：当前 11 段脚本的前 4:20 虽然引用真实封存证据，但主要通过 PowerShell 安全字段投影、
JSON 结果与静态指标卡表达。现有 `console/` 擅长身份、审批、Effect、Undo 与证据闭环，
`frontend/` 擅长静态审计回放；两者都没有把一次 Agent 工具调用经过 Gate1–6、形成最终判定并改变
业务后果的过程直观串起来。若直接按现稿录制，容易呈现为“全脚本输出”，Gate1–6 的运行感与
OpenClaw 类工具调用应用的兼容价值不足。

**赛事与仓库依据**

1. 赛题方案要求演示视频展示“原型系统核心功能、关键技术流程及测试效果”，并在选题意义中提出
   “兼容 OpenClaw 类智能体及其他具备工具调用能力的大模型应用”；
2. `事实源.md` / `PRD.md` 保留“至少一次 TRAE 真实客户端跑通”的承诺；TRAE 原生 MCP elicitation
   未声明支持，不等于可以退役 TRAE 的基础 MCP 连接、工具发现与调用验证；
3. 既有 OAR Null SUT + GullibleSeat 是确定性阻断证明，不等同于真实模型易感性证明；新增 live-agent
   轨道不得改旧 Oracle，而应把真实模型产生的同一危险 `ToolIntent` 固化后再做 Null/XA-Guard 因果分叉；
4. 已封存 OAR A/B、AP-D2、AP-D3 与 Console 证据可作为可视化数据源，但必须标明是实时运行还是
   封存运行回放。

**已完成的框架范围**

- `kernel/live_agent/` 已提供 DeepSeek adapter、D1–D3 policy intent matcher、双执行器、独立 verdict、
  freeze/evaluate/render CLI 与自包含 `replay.html`；不会把模型安全拒答记作 XA-Guard 成功；
- `.env.example` 与本地忽略的 `.env` 已预留 DeepSeek V4 Pro 非思考模式配置，密钥不进入证据；
- 新框架 + range CLI 22 项通过，Ruff 通过；真实 SUT 项曾通过一次，收尾复跑连续两次进程启动超时，
  失败按原测试保留且未改测试；一次 development 单次烟测仅验证真实多轮 Tool Calls，使用 offline guard，
  含 2 个工具参数错误，**不作为正式攻击或防御证据**；
- D1–D3 清单只保留合成目标、工具面、development/holdout 隔离与 TODO，不由本框架作者设计攻击脚本。

**待闭合范围**

- 至少一个真实、具备工具调用能力的 Agent 客户端完成 XA-Guard MCP 连接、工具发现和实际调用实拍；
  优先验证本机已安装的 TRAE SOLO，若其版本能力不足，须如实记录并按事实源允许的兼容边界选择替代载体；
- 提供可视化运行视图，把 Agent 活动、实际 Gate 状态、最终 decision/reason/trace 与业务世界后果
  串成同一条因果链；可采用实时模式或明确标识的封存证据回放；
- 复用现有 Console 展示身份、审批、Effect、Undo 与双链证据，不重复建设完整管理后台；
- 由 Kimi 补齐并审阅 D1–D3 development/holdout payload，冻结后不得反向调参；
- 用 OAR Null SUT 表示裸奔侧、真实 `XaGuardSUT(live=True)` 表示受保护侧，至少完成一组达到 3/5
  门槛的同 `property + target` 因果 A/B；框架 smoke 或 offline guard 不得代替；
- 将真实 Gate audit 映射到回放页；当前页已提供 Gate1–6 位置与最终 decision，但未宣称六 Gate 均有逐 Gate 状态。

**真实性红线**

- 不为画面效果伪造 Gate1–6 全部命中；每个 Gate 只能显示真实的 `PASS / HIT / DENY / NOT REACHED`
  或等价状态，且必须能回溯到运行数据；
- 不把封存回放冒充现场实时运行，不把静态 mock 冒充真实 Agent 工具调用；
- 不改测试、断言、阈值、攻击 case oracle 或冻结数字；不泄露 raw payload、token、个人信息和绝对路径；
- TRAE/其他客户端不支持原生 elicitation 时，使用 pending fallback 或 Console 审批，并明确能力边界，
  不把客户端限制描述成 XA-Guard 已实现能力。

**验收**

1. 不录制预演中可看到至少一个真实 Agent 客户端完成工具发现和一次实际调用；
2. 同一任务的 Null / XA-Guard A/B 可视化能显示不同最终 decision 与可核验业务后果；
3. 画面至少关联实际 Gate 状态、reason、trace ID 和下游执行/外发计数，不只展示最终 JSON；
4. 身份/审批/Undo/证据链继续由现有 Console 承担，并能与 Agent 调用 trace 互相定位；
5. D3 脚本、旁白和 SRT 按最终载体二次同步，通过一次完整不录制预演后才允许正式录制。

---

## 4. P1 任务（提升可信度）

### P1-1　OAR live A/B 提 N 至 10（DEC-3 已定案）✅ **DONE 2026-07-27**

**结果**：run `xa-attack-proof-v1-20260727T033934Z-win-local` PASS（6/6 verified、0 infra_error）；
邮箱/RAG 各 Null 10/10 泄漏、XA-Guard 0/10；protected replay **20/20**；根清单 730/730 含 40 个子清单；
tarball SHA-256 `435d170585e2266a65107db1bba3ee2705b5cfec8b29d76948d59898026e8e5b`；
公开导出 `docs/evidence/attack-proof-set-2026-07-27-n10/` 六件套（独立目录复跑字节级一致）；
全部引用点已同步，`FROZEN-NUMBERS.md` 已建立。旧 N=3 run 保留为历史。

**过程透明记录**：首次运行（`...20260727T032104Z...`）AP-D1-MAIL 第 8 轮、第二次（`...20260727T033000Z...`）
AP-D1-RAG 第 1 轮各遇一次 harness 子进程启动挂起（60s 就绪超时，发生在任何攻击执行之前，非防护失败），
两次均以 LIMIT 封存保留。随后对 OAR harness `kernel/sut.py` 做唯一一处修复（live 会话启动重试一次；
非产品代码、非阈值、非断言、非 case oracle；重试透明记录于 `sut-session.json`），第三次运行 PASS，
20 个 protected 会话中 2 个实际使用重试。负责人已知情并批准补跑与修复路径。

<details><summary>原任务定义（历史）</summary>

N=3 在 30% 维度上读起来偏薄。两个注入 case（`AP-D1-MAIL`、`AP-D1-RAG`）**每侧 N=10**，
一次性重跑并重新封存证明集。

**约束**

- 必须沿用现有 clean-run 流程：`--require-clean`、start/end Git clean、根逐文件清单、源码 provenance、公开脱敏导出；
- **只跑一次**。跑完即冻结数字，之后不得因结果不好看而重跑；
- 不改阈值、断言、产品代码或 case oracle——只改 N；
- 若 protected 侧出现非零泄漏或 infra error，**照实记录并写入 D1**，按真实值调整表述，不重跑掩盖。

**验收**：新 run id 记入 `docs/evidence/`；tarball SHA-256、根清单覆盖数、protected replay（预期 20/20）全通过；
`status.md` / Delivery v2 / Evidence Consolidation / D1 / D3 字幕 / `FROZEN-NUMBERS.md` 同步为新数字；
旧 N=3 run 保留为历史，不覆盖、不伪装。

</details>

### P1-2　消化免责声明密度　✅ **DONE 2026-07-27**

已完成：新增 v0.2 **§5.9「能力边界与不声明事项」**集中框（识别指标/策略判定/证明集与实验/兼容与部署/证据公开五组）；
§5.1/§5.2/§5.7/§3.7/§6.2/§6.4/§7 正文改为“结果 + 适用范围”表述并指向 §5.9；Gate1 四条边界与
OpenClaw 两条不声明事项逐字保留；数字零改动；构建 17 页通过。

<details><summary>原任务定义（历史）</summary>

把散落的"不宣称/不外推/已退役"收进单一"能力边界与不声明事项"框；
正文改为"结果 + 适用范围"表述，删除"我们没有 X"式句子（如 v0.2 第 148 行第二句）。
**边界内容一条不减，只改位置与句式。**

</details>

### P1-3　用满页数预算

P0-1..P0-5 已全部落地，临时构建为 16 页，距 30 页上限仍有 14 页余量。后续按可读性而非凑页数微调。

### P1-4　赛事咨询（DEC-4 已定案，不再阻塞）

- **Q6 视频格式**：负责人已问过赛事方，答复**无特殊要求** → D3 沿用现有参数（§7），风险 R6 关闭。
- **Q1 邮件命名**：赛题 PDF 第 6 页原文为 `[揭榜挂帅·心理智能体]+学校+团队负责人姓名`（疑模板错误）。
  处置口径已定：**主题按 PDF 原文照写，正文首行写明题号 XA-202620 与完整题目名称**，无需等待答复。
- 事实源 §8 其余 Q2–Q5、Q7、Q8 降为可选，不阻塞任何交付项。

---

## 5. P2 任务（答辩与掌控力，11 月才用但现在做）

### P2-1　答辩问答手册（≈30 题，每答案指到仓库具体文件）

按提问概率排序，起始题目：

| # | 题 | 答案锚点 |
|---|---|---|
| 1 | 攻击识别准确率多少？误报率多少？ | P0-1 分层表 + `gate1_scope` |
| 2 | N=3 是否太少？ | P1-1 结果 / 或二项 CI |
| 3 | Gate1 是规则还是模型？规则易被绕过吗？ | Detector 接口 + `detectors_summary` |
| 4 | Undo 能撤什么、不能撤什么？ | v2 恢复合同 + `manual_required` |
| 5 | 45ms 增量相对什么基线？ | `protected_ms - direct_business_baseline_ms` |
| 6 | 与 Lakera / LlamaFirewall / CaMeL 差别？ | P0-5 对比表 |
| 7 | 290 条 seed 自写？如何排除过拟合？ | `independent_holdout=false` + 指纹零重叠切分 |
| 8 | 为何相信 Gate6 哈希链？私钥谁持有？ | SM2-with-SM3、key id、`.runtime` 隔离 |
| 9 | 0.3575 那个数字怎么解释？ | 归属误配，见 P0-1 拆解 |
| 10 | 为什么 kind 不等于生产 HA？ | `LOCAL-PROFILE-PASS` 边界 |

### P2-2　`CODE-MAP.md`

D1 每条论断 → 实现文件:行 → 证明它的测试。约两小时工作量，同时满足赛题第 2 项
"须可复现关键技术验证结果，并提供运行说明"，并降低"vibe 代码掌控力低"的答辩风险。

### P2-3　`FROZEN-NUMBERS.md`

单一数字源。D1、D3 字幕、submission-checklist、status.md 全部引用它，消除口径漂移。

---

## 6. 冻结顺序与排期

**依赖链是「数字 → 视频 → PDF」，不是「视频 → PDF」。**
数字是最上游约束：视频录完后若数字变动，只能重录或接受口径不一致
（submission-checklist 第 54 行已把口径漂移列为红线）。

DEC-3 已定案提 N，故**数字冻结日锁定 8/9，排期无左移空间**。

| 周 | 任务 | 冻结点 |
|---|---|---|
| ~~7/26~~ | ~~DEC-1..5 定案~~ + ~~P0-1..P0-6 全部完成~~ | ✅ **决策已冻结**（§1）；当前 v0.2 = 16 页 |
| **7/27** | ~~P1-1 OAR N=10 重跑 + 重新封存~~（**提前完成**）+ 同步全部引用点 + 建 `FROZEN-NUMBERS.md` | ✅ **数字已冻结（2026-07-27）** |
| **7/27–8/2** | ~~P1-2 边界收框~~（7/27 完成）；~~最终通读压口径~~（7/27 完成） | — |
| ~~8/3–8/9~~ | ~~P1-1 唯一一次 OAR 重跑~~（已提前至 7/27 完成） | — |
| **8/10–8/23** | D1 内容完成 → v0.9 PDF；通读；**只改文字不改数字**；P2-1/P2-2 并行；闭合 D3-P0-1 的载体选型、实现、真实客户端验证与不录制预演 | ✅ **D3 可录制前置闭合** |
| **8/24–9/6** | 录制 D3（§7，真人旁白）；每个上屏数字对 `FROZEN-NUMBERS.md`；记录录像暴露的 D1 待改项 | ✅ **视频定版** |
| **9/7–9/11** | 只把录像暴露的问题回改 D1；重建 PDF；核页数、hash、checklist | ✅ **PDF 冻结** |
| **9/12–9/14** | 提交邮件 + 附件 + 网盘权限核对 | ✅ **提交** |

~~**8/9 之前不得把 N=10 写成已完成结果**~~ 数字已于 2026-07-27 真实重跑并冻结，
D1/D3 直接引用 `FROZEN-NUMBERS.md` 的 N=10 冻结值。

**为什么现在不冻 PDF**：录像会逼出讲不圆的论断和含糊表述，这些是最值得改的；且 30 页预算尚空 14 页。
**为什么也不提前重建正式 PDF**：内容未定就重建会产生多个版本的 PDF 与 hash，
正式重建统一放在 8/10–8/23 内容完成之后；在那之前 `output/pdf/` 的 14 页版本作为兜底保留。

---

## 7. D3 镜头重排

**镜头结构重排已完成（2026-07-26），但 D3-P0-1 可视化载体仍 OPEN。**
[D3-video-script.md](D3-video-script.md) 已改为下列 11 段镜头，
新增前 4:20 的 live A/B、Gate1 量化、高风险命令与 AIBOM；身份、Effect、Undo 与证据后移，
工程部署压缩。精确真人旁白已独立为 [D3-video-voiceover.md](D3-video-voiceover.md)，
SRT 已按 9:30 时间线重写。当前前半段仍偏终端安全投影/静态卡，须先按 D3-P0-1 补真实客户端和
Agent→Gate→业务后果可视化，再二次同步脚本、旁白与 SRT。不得把脚本结构完成写成可录制或视频定版。

| 时间 | 镜头 | 要点 | 方向 |
|---|---|---|---|
| 0:00–0:40 | 冷开场 | 题号 + 一句话价值 + 四方向覆盖图。**不要用 50 秒开身份** | — |
| 0:40–2:10 | **live A/B（全片核心）** | 同一封注入邮件：Null 侧 `cit-1001` 真实外发；XA-Guard 侧执行前 deny、外发 0 | 一 |
| 2:10–2:50 | 识别量化 | P0-1 分层表上屏：60/60、FPR 0/58、p95 0.04ms + 范围声明 | 一 |
| 2:50–3:40 | 高风险命令 | require_approval → deny（下游 0）；批准对照 → allow（下游 1）。同时证明**能拦**与**不过度拦** | 二 |
| 3:40–4:20 | AIBOM | 恶意插件 deny + 干净 artifact 准入，露出 A–F 评级 | 三 |
| 4:20–5:10 | 身份与委托 | Alice Console 的 human→Agent→tool→domain；伪造 header/撤权 → 执行前失败关闭，`business_create_attempt_delta=0` | 横向 |
| 5:10–6:00 | 真实副作用 | 创建工单；Effect prepared→available；`effect_id` 幂等键 | 二 |
| 6:00–7:10 | 职责分离 Undo | Alice 申请（NO APPROVER ROLE）→ Dora 独立会话批准 → Worker 补偿 → COMPENSATED，open→cancelled | 二 |
| 7:10–8:10 | 双链证据 | 两个 trace 交叉引用；**clean 验签 exit 0 / tampered 验签 exit 1** | 四 |
| 8:10–9:00 | 工程与部署 | compose 六服务 healthy、kind 七阶段、11/11 fault、p95 表（**压缩，不逐条展开**） | — |
| 9:00–9:30 | 边界与收束 | 声明边界、commit、题号 | — |

**相对现行脚本的增删**：新增 0:40–4:20 的四方向实拍段；kind 七阶段与 compose ps 由逐条展示压缩到约 15 秒合计。

**已冻结的成片参数**（DEC-4：赛事方答复无特殊要求，故沿用现有参数）

| 项 | 值 |
|---|---|
| 分辨率 / 宽高比 | 1920×1080，16:9 |
| 帧率 | 30 fps |
| 编码 | H.264 + AAC（`-crf 20`、`-b:a 160k`、`+faststart`） |
| 时长 | 目标 **9:00–9:30**，硬顶 10:00 |
| 字幕 | 烧录（`subtitles=` 滤镜）；新 SRT 已完成（38 cues，最后一条结束于 9:29.500） |
| **旁白** | **真人录音（DEC-5）**，不使用 TTS |
| 文件名 | `XA-Guard-XA-202620-demo.mp4` |

**真人录音的连带要求**（DEC-5）

- 离屏旁白稿已按新镜头表完成；自动按 190 字/分钟做保守预算，仍须负责人**逐段真人试读掐表**；
- 每段录音与画面分开录、后期对齐，比一边操作一边讲更容易一条过；
- 统一麦克风与房间，避免分段之间音色跳变；录前后各留 2 秒静音便于剪辑；
- `D3-video-subtitles.srt` 已按新顺序重写；真人试读若改词，须同步修改旁白稿与 SRT。

**其余制作要点**

- **clean/tampered 验签对照那 30 秒必须留**——一个 exit 0 一个 exit 1，是"审计溯源完整性"最省解释的画面。
- 屏幕上每个数字必须等于 `FROZEN-NUMBERS.md`；数字已于 2026-07-27 冻结，可以进入预演。
- 分 10–12 个 ≤90 秒短片段录，每段一个概念；**先做一次完整的不录制预演**专门找 UI 崩点。
- 终端里露一次 `git rev-parse HEAD` 且与 D1 PDF 上的 commit 一致——成本近零，可信度加成大。
- 新录制指南 §2 环境准备、§5 失败恢复、§6 合成与验收已随 11 段镜头同步校正。

---

## 8. 风险登记

| # | 风险 | 影响 | 处置 | 状态 |
|---|---|---|---|---|
| ~~R1~~ | ~~PDF 管线在 9 月才发现构建不了目标稿~~ | ~~致命~~ | P0-6 已于 2026-07-26 完成：v0.2 baseline → 13 页、P0 全量稿 → 16 页、v1 → 14 页未回归、`py_compile` PASS | ✅ **CLOSED 2026-07-26** |
| R2 | 视频数字与 D1 不一致 | 高（红线） | 数字已冻结（2026-07-27）+ `FROZEN-NUMBERS.md` 单一来源；D3 字幕逐项对表 | MITIGATED |
| R3 | OAR 重跑破坏已封存 provenance | 高 | 已沿用 `--require-clean` 流程完成；两次 LIMIT run 与旧 N=3 run 全部封存保留；harness 修复透明记录 | ✅ **CLOSED 2026-07-27** |
| R4 | 补写指标时越界宣称 | 高（答辩被拆） | P0-1 四条边界缺一不可；对比表只用事实源已核实条目 | OPEN |
| R5 | 掌控力不足导致答辩失分 | 中高 | P2-1 问答手册 + P2-2 CODE-MAP + 录像预演 | OPEN |
| ~~R6~~ | ~~咨询邮件回复晚于录像~~ | ~~中~~ | DEC-4：视频格式已问过，答复无特殊要求；邮件命名按 PDF 原文照写，无需等答复 | ✅ **CLOSED 2026-07-26** |
| R7 | 为补指标而临时改产品 | 中 | 红线：不改安全语义、阈值、断言或 case oracle；D3-P0-1 只允许演示专用可视化与真实客户端验证 | OPEN |
| R8 | D4 报名表状态 | 致命 | 报名系统 5/30–6/30 开放，checklist 记 2026-07-18 负责人确认审核通过；**建议再核实一次审核状态截图** | OPEN（需人工核实） |
| **R9** | 真人旁白读超时长 | 中 | DEC-5 连带：旁白稿逐段试读掐表；真人语速常比预估慢 10–15%，按 9:00 写稿而非 9:30 | OPEN |
| **R10** | D3 仅靠终端/静态卡，Gate1–6 与真实 Agent 接入显得空泛 | 高 | D3-P0-1：补真实工具调用客户端 + 可回溯的 Gate/业务后果可视化；封存回放须明示，禁止伪造 Gate 状态 | 🔴 **OPEN / 正式录制前阻塞** |

---

## 9. 进度勾选

**决策（2026-07-26 全部定案，不再回退）**
- [x] DEC-1 底稿 = **v0.2 审阅稿**
- [x] DEC-2 Gate1 分层指标 = **写入**
- [x] DEC-3 OAR N = **3 → 10**
- [x] DEC-4 咨询 = **视频格式已问，无特殊要求**
- [x] DEC-5 旁白 = **真人录音**

**P0**
- [x] **P0-6 构建管线 + 切 SOURCE 到 v0.2**（2026-07-26；baseline v0.2 = **13 页**，P0 全量稿 = **16 页**，v1 = 14 页未回归）
- [x] **P0-1 Gate1 分层指标**　- [x] P0-2 指标体系　- [x] P0-3 算法设计
- [x] **P0-4 OpenClaw 兼容边界**　- [x] **P0-5 横向对比表**
- [ ] **D3-P0-1 可视化运行载体 + 真实 Agent 客户端演示**（框架/GUI ✅；Kimi 攻击 TODO、正式 live 评估、逐 Gate audit 映射与镜头同步仍 OPEN）
- [ ] 内容完成后用 v0.2 重建正式 `output/pdf/`（安排在 8/10–8/23，最终页数：____）

**P1**
- [x] **P1-1 OAR N=10 重跑并重封存**（2026-07-27，新 run id：`xa-attack-proof-v1-20260727T033934Z-win-local`）
- [x] **P1-2 边界收框**（2026-07-27，v0.2 §5.9 集中框）　- [ ] P1-3 页数用满
- [x] ~~P1-4 咨询邮件~~（DEC-4 已解决，无待办）

**P2**
- [ ] P2-1 问答手册　- [ ] P2-2 CODE-MAP　- [x] P2-3 FROZEN-NUMBERS（2026-07-27 提前建立）

**D3 连带**
- [x] 11 段镜头**结构**重排 + 录制操作/安全投影/合成验收重写（2026-07-26；可视化载体落地后须二次同步）
- [ ] D3-P0-1 载体选型、实现、真实客户端验收与镜头二次同步
- [x] 真人旁白稿按新镜头表重写并完成 190 字/分钟保守预算　- [ ] 负责人逐段真人试读掐表
- [x] `D3-video-subtitles.srt` 重写（38 cues，时间轴/重叠/行长校验通过）
- [ ] 不录制预演完成，UI 崩点清单：____

**冻结**
- [x] 决策冻结（2026-07-26）
- [x] 数字冻结（目标 8/9，实际：**2026-07-27**）　- [ ] 视频定版（目标 9/6，实际：____）
- [ ] PDF 冻结（目标 9/11，实际：____）　- [ ] 已提交（目标 9/12–14，实际：____）

---

*维护：本计划变更时同步 `status.md` 与 `log.md` 顶条；已冻结项不得回退为待办。*
