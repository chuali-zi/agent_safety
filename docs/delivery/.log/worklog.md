# docs/delivery 工作日志

## 2026-07-27 D1 最终通读压口径

全文通读 v0.2 并对照事实源/FROZEN-NUMBERS 核验：§6.5 外部数字与 F-4.1/F-4.4/F-5.2 全部一致。压口径 4 处：头部日期→2026-07-27、摘要“同一攻击任务”→两类注入 finding、摘要时延句加“在上述验证规模内”、§5.4 full-day 补 canonical run 归属。构建 17 页不变。下一步：D3 旁白稿/SRT 重写、正式 PDF 重建。

## 2026-07-27 P1-1 N=10 冻结 + P1-2 边界收框完成

P1-1：OAR live A/B 重跑为每侧 N=10。前两次 run 各遇一次 harness 子进程启动挂起（LIMIT，已封存保留）；修复 `kernel/sut.py` 增加唯一一次启动重试后，第三次 run `xa-attack-proof-v1-20260727T033934Z-win-local` PASS（6/6、replay 20/20、根清单 730/730、tarball `435d1705...8e5b`）。公开导出六件套复跑字节级一致；新叙述页 `docs/evidence/attack-proof-set-2026-07-27-n10.md` 建立，旧 N=3 页转为历史；D1/D3/checklist/DELIVERY-v2/EVIDENCE-CONSOLIDATION/status/handoff 全部同步；新建 `FROZEN-NUMBERS.md` 单一数字源，数字冻结（提前于原 8/9）。

P1-2：子 agent 执行、主 agent 验收。v0.2 新增 §5.9「能力边界与不声明事项」集中框，正文改“结果 + 适用范围”表述；边界一条未减、数字零改动；临时构建 17 页。下一步：最终通读、D3 旁白稿与 SRT 重写、正式 PDF 重建。

## 2026-07-26 D1 六项 P0 全部闭合

补入 §6.2 OpenClaw 协议层兼容边界与 §6.5 横向对比表，外部方案“未核验”不写成“没有”；补参考文献和验证索引。P0 全量稿临时构建 16 页，v1 兼容 14 页，完成页 13–16 抽检。未替换正式 PDF，未改产品、测试或证据。

## 2026-07-26 D1 P0-1/P0-2/P0-3 源稿补写

补写 v0.2 §3.3 算法设计、§5.1 指标体系与预期效果、§5.2 Gate1 分层识别；纠正 FPR 为 0/58，OAR N=3 标为草稿值。临时构建 15 页，未改产品、测试、证据或正式 PDF。下一步 P0-4/P0-5。

## 2026-07-26 D1 评分维度审阅 + 提交材料收口计划

对照赛题 PDF（创新 25 / 效果 30 / 完整 20 / 价值 20 / 表达 5）复核两份 D1 稿与 D3 脚本。
未改产品、测试、证据或 D1/D3 正文，未重跑实验。

产出：[D1-D3-submission-plan.md](../D1-D3-submission-plan.md)（决策 5、P0 6、P1 4、P2 3、镜头表、风险 8）、
[REVIEW-2026-07-26.md](../REVIEW-2026-07-26.md)（负责人视角）；同步 `status.md`、`log.md` 顶条、
`submission-checklist.md` 与 `D3-video-script.md` 顶部提示。

三条主要结论：

1. **v0.2 稿当前构建不出 PDF**——无 pagebreak 标记、用 mermaid 而非 `[DIAGRAM:x]`，
   `scripts/build_d1_pdf.py:154` 直接 ValueError（已实测）。列为 P0-6，须第一周修。
2. **占 30% 的"实际效果"缺赛题点名的识别率与误报率**（两稿 grep 均 0 次），
   但 `gate1-l3-evaluation-2026-06-18.json` 的 `gate1_scope` 已有数字：60 例召回 1.0、
   FPR 0/58（Wilson 上界 6.21%）、p95 0.04ms。总体 0.3575 是归属误配——
   193 例中 133 例属治理类，由 Gate2/3/5+AIBOM 裁决而非 Gate1。改为分层表 + 四条范围声明。
3. **D3 现行八镜头方向一/三 0 秒出镜**，无 live 攻击拦截与 AIBOM 画面。前 4 分钟重排为四方向实拍。

冻结依赖链：**数字 → 视频 → PDF**。8 月初一次性重跑后冻数字，8 月底录像，9 月初冻 PDF，9/12–14 提交。

## 2026-07-26 五项决策定案与冻结

负责人拍板：① 底稿 = **v0.2 审阅稿**（v1 转历史参考，不再作构建源）；② Gate1 分层指标**写入**；
③ OAR N **3→10**；④ 视频格式**已问，无特殊要求**；⑤ 旁白**真人录音**。

三条连带口径已写入计划与 status：

1. **P0-6 升为硬前置**且须排在其余 P0 之前——v0.2 构建通前 D1 无最终产物，14 页 PDF 仅过渡兜底不删。
2. **数字冻结日锁定 8/9**，此前 D1 与字幕中 OAR 数字留占位不写定值；重跑只改 N，不改阈值/断言/oracle，
   只跑一次，protected 侧异常照实记录不掩盖。
3. 风险登记：**R6 关闭**（咨询已解决）、**R1 升级致命**、**新增 R9**（真人旁白读超时长，按 9:00 写稿）。

D3 连带待办：旁白稿与 `D3-video-subtitles.srt` 需按新镜头顺序重写。

## 2026-07-26 P0-6 完成核实 + 页数余量修正

并行会话已实现 P0-6（`scripts/build_d1_pdf.py`：切 SOURCE 到 v0.2、pagebreak 容错、
离线 Mermaid 渲染、保留 `[DIAGRAM:x]` 兼容 v1）。该改动已从决策冻结提交中摘出单独处理。

独立复核（临时输出路径，未覆盖 `output/pdf/` 兜底）：`py_compile` PASS、
v0.2 → **13 页**、v1 → 14 页未回归。

**关键修正：13 页远低于原预估 22–26 页，余量 17 页。** 原"接近上限需压缩"的判断作废；
P0-1..P0-5 可放开写，落地后约 21 页仍留 9 页缓冲。R1 关闭，**下一动作 = P0-1**。

正式 `output/pdf/` 不立即重建——统一放到 8/10–8/23 内容完成后一次性重建，避免多版本 hash。
