name: Quality

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.12"]
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e ".[crypto,bench,dev,policy,aibom,http]"
      - run: python -m ruff check src bench demo scripts tools open-agent-range/kernel --exclude open-agent-range/kernel/tests
      - run: python -X faulthandler -m pytest -vv
      - run: python -X faulthandler -m pytest -vv open-agent-range/kernel/tests
        env:
          PYTHONPATH: open-agent-range
